services:
  worker:
    build:
      context: ../..
      dockerfile: apps/worker/Dockerfile
    ports: ["7860:7860"]
    env_file: ../../.env
    volumes: ["hf-cache:/root/.cache/huggingface"]
volumes: {hf-cache: {}}
